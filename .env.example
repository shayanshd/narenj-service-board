# Server-only. Never expose this value to browser code or commit a real key.
OPENAI_API_KEY=

# Low-latency model for the bounded manager handover. Override after evaluation if needed.
OPENAI_MODEL=gpt-4.1-mini
